import type { DelayRange } from './types';

export const DEFAULT_TYPE_SPEED: DelayRange = [50, 90];

export const DEFAULT_DELETE_SPEED: DelayRange = [30, 60];

export const DEFAULT_SENTENCE_DELAY = 2000;

export const DEFAULT_LOOP_DELAY = 1000;

export const DEFAULT_BLINK_SPEED = 530;
